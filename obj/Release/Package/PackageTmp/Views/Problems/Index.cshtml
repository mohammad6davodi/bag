@model IEnumerable<UploadAndDisplayImageInMvc.Models.Problem>

@{
    ViewBag.Title = "حل مشکلات";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="bb">
    <a href="~/Content/Place"><button>برگشت</button></a><br />
</div>
@foreach (var item in Model)
{
    <div class="pr">
        <h1>یک مشکل داریم</h1>
        <h4>@item.FullName</h4>
        <h4>@item.Email</h4>
        <button style="background-color:green; border-color:green; "><a href="~/Problems/Details/@item.ID">جزئیات</a></button>    <button style="background-color:red; border-color:red;"><a href="~/Problems/Delete/@item.ID">حذف</a></button>
    </div><br />
}
<style>
    body {
        background-image: url(../../img/mynight.jpg);
        background-repeat: no-repeat;
        background-size: cover;
        background-attachment: fixed;
    }

    .pr {
        border-style: solid;
        border-radius: 20px;
        border-color: powderblue;
        padding: 6px;
        margin: 6px;
        text-align: center;
        background-color: powderblue;
        box-shadow: 0 4px 8px 0 powderblue, 0 6px 20px 0 powderblue;
    }

        .pr a {
            text-decoration: none;
        }

        .pr button {
            font-size: 160%;
            width: 160px;
        }

    a {
        text-decoration: none;
        color: white;
    }

    button {
        border-radius: 26px;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
    }
</style>